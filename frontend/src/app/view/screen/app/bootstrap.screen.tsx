import React, { FunctionComponent } from 'react';
import Box from '@material-ui/core/Box';

export const BootstrapScreenComponent: FunctionComponent<object> = (props) => {
	return (
		<Box>
			Loading
		</Box>
	);
};

export { BootstrapScreenComponent as BootstrapScreen };
